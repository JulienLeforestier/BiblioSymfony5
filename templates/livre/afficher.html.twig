{% extends 'base.html.twig' %}

{% block title %}Livre : Affichage
{% endblock %}

{% block body %}

	<h2>Affichage du livre n°{{ livre.id }}
	</h2>

	<ul class="list-group">
		<li class="list-group-item">
			<strong>Titre :
			</strong>
			{{ livre.titre }}
		</li>
		<li class="list-group-item">
			<strong>Auteur :
			</strong>
			{{ livre.auteur }}
		</li>
	</ul>
	<br>
	<a href="{{ path("profil_emprunter", {id: livre.id}) }}" class="btn btn-primary {{ livre in liste_livres_indispos ? 'disabled' : '' }}">Emprunter</a>
	<br><br>
	<a class="btn btn-primary" type="cancel" href="{{ path('livre') }}">Retour</a>

{% endblock %}
